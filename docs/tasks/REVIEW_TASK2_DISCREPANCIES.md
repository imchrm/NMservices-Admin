# Ревью Задачи 2 — Несоответствия реализации

**Дата ревью:** 2026-02-13
**Документ-источник:** `docs/tasks/session-context-admin-panel.md`
**Общий план:** `docs/tasks/MVP_PLAN.md`

---

## Общее состояние

- **Тесты:** 30/30 passed
- **TypeScript:** компиляция без ошибок (`tsc --noEmit` — OK)
- **Проект инициализирован:** Vite + React + TypeScript + react-admin — всё на месте

---

## Сводка по подзадачам

| Подзадача | Статус | Примечания |
|-----------|--------|------------|
| 2.1. Инициализация проекта | ✅ Готово | — |
| 2.2. Dashboard | ⚠️ Частично | Количество услуг берётся не из `/admin/stats` |
| 2.3. Услуги | ⚠️ Частично | Нет явной кнопки деактивации |
| 2.4. Заказы | ❌ Есть баги | Баг в ссылке на услугу + нет фильтра по дате |
| 2.5. Пользователи | ✅ Готово | Плюс есть UserCreate (не требовался, но не мешает) |

---

## Детальное описание несоответствий

### 1. 🔴 БАГ: OrderShow — неверная ссылка на ресурс услуг

**Файл:** `src/orders/OrderShow.tsx:17`
**Серьёзность:** Высокая (функциональный баг)

```tsx
<ReferenceField label="Service" source="service_id" reference="services" link="show">
```

**Проблема:** Ресурс `services` **не зарегистрирован** в `App.tsx`. Зарегистрирован только `admin/services`. При попытке отобразить заказ с `service_id`, react-admin будет делать запрос на `/services/{id}` с заголовком `X-Admin-Key`, но этот endpoint требует `X-API-Key` → ошибка 403.

**Исправление:** Заменить `reference="services"` на `reference="admin/services"`:
```tsx
<ReferenceField label="Service" source="service_id" reference="admin/services" link="show">
```

---

### 2. 🟡 Заказы: отсутствует фильтрация по дате

**Файл:** `src/orders/OrderList.tsx`
**Серьёзность:** Средняя (неполный функционал)

**Спецификация (п. 2.4.1):**
> Список заказов (таблица с фильтрацией по статусу, **дате**)

**Реализация:** Есть только фильтр по статусу (`SelectInput` для `status`). Фильтр по дате (`created_at` или `scheduled_at`) отсутствует.

**Примечание:** Нужно уточнить — поддерживает ли backend API фильтрацию по дате для `/admin/orders`. Если нет, потребуется сначала добавить поддержку на backend.

---

### 3. 🟡 Dashboard: количество услуг не из `/admin/stats`

**Файл:** `src/dashboard/Dashboard.tsx:47-58`
**Серьёзность:** Низкая (workaround работает)

**Спецификация (п. 2.2.1):**
> Статистика: кол-во пользователей, заказов, услуг (GET /admin/stats)

**Реализация:** Endpoint `GET /admin/stats` возвращает только `{total_users, total_orders, orders_by_status}` — **без `total_services`**. Dashboard делает дополнительный запрос на `GET /admin/services?limit=1`, чтобы получить `total` из ответа.

**Это ограничение backend**, а не admin panel. Два варианта решения:
1. Добавить `total_services` в ответ `/admin/stats` на backend (предпочтительно)
2. Оставить текущий workaround (работает, но лишний запрос)

---

### 4. 🟡 Услуги: нет явной кнопки деактивации

**Серьёзность:** Низкая (есть обходной путь)

**Спецификация (п. 2.3.5):**
> Деактивация услуги (soft delete)

**Реализация:** Деактивация возможна **только через Edit** (переключатель `is_active`). Нет выделенной кнопки "Деактивировать" в списке или карточке просмотра.

Backend `DELETE /admin/services/{id}` выполняет soft delete (ставит `is_active=false`), и react-admin может показать стандартную кнопку Delete в Edit-форме, но UX не оптимален для этого сценария.

---

### 5. 🔵 Документ session-context-admin-panel.md — устарел

**Серьёзность:** Информационная

Документ `session-context-admin-panel.md` **не содержит** эндпоинтов `/admin/services/*` в карте API. Он описывает только `/services/*` (с `X-API-Key`). Однако backend **уже имеет** полный набор admin-эндпоинтов:

| Method | Path | Auth |
|--------|------|------|
| GET | `/admin/services` | X-Admin-Key |
| GET | `/admin/services/{id}` | X-Admin-Key |
| POST | `/admin/services` | X-Admin-Key |
| PATCH | `/admin/services/{id}` | X-Admin-Key |
| DELETE | `/admin/services/{id}` | X-Admin-Key |

Admin Panel **правильно** использует `admin/services` как resource name (см. `App.tsx:39-47`), что маппится на `/admin/services/*` с корректным заголовком `X-Admin-Key`. Но документ нужно обновить, чтобы отразить это.

Замечание в документе:
> **Услуги используют другой auth-заголовок**
> `/services/*` — использует `X-API-Key` (не X-Admin-Key)
> DataProvider должен отправлять правильный заголовок в зависимости от endpoint

Это замечание **больше не актуально** для Admin Panel, т.к. все запросы идут через `/admin/*` с единым `X-Admin-Key`. Единственное исключение — баг в OrderShow (п. 1 выше).

---

## Что реализовано корректно

### 2.1. Инициализация проекта ✅
- [x] Vite + React + TypeScript
- [x] react-admin + MUI зависимости
- [x] dataProvider: пагинация (skip/limit), сортировка (sort_by/order), фильтрация (status_filter), unwrap ответов
- [x] authProvider: X-Admin-Key через localStorage

### 2.2. Dashboard ✅ (с оговоркой)
- [x] Кол-во пользователей и заказов (из /admin/stats)
- [x] Кол-во услуг (из /admin/services — workaround)
- [x] Заказы по статусам с цветными чипами

### 2.3. Услуги ✅ (с оговоркой)
- [x] Список с фильтрацией (по имени, include_inactive)
- [x] Просмотр (детальная карточка со всеми полями)
- [x] Создание (форма: name, description, price, duration, is_active)
- [x] Редактирование (все поля)
- [ ] Деактивация как отдельное действие (только через Edit)

### 2.4. Заказы ⚠️
- [x] Список с фильтрацией по статусу
- [ ] Фильтрация по дате — **не реализована**
- [x] Просмотр (user, service, address, status, notes, timestamps)
  - ⚠️ Ссылка на услугу **сломана** (reference="services" вместо "admin/services")
- [x] Редактирование (status, total_amount, notes)
- [x] Создание (user_id, status, amount, notes — без service_id, т.к. ограничение API)

### 2.5. Пользователи ✅
- [x] Список (таблица со всеми полями)
- [x] Просмотр (профиль + история заказов через ReferenceManyField)
- [x] Создание (бонус — не требовалось в спеке, но не мешает)

---

## Рекомендуемый приоритет исправлений

1. **🔴 Исправить баг OrderShow** — `reference="services"` → `reference="admin/services"` (1 строка)
2. **🟡 Добавить фильтр по дате** в OrderList (потребуется проверить поддержку на backend)
3. **🟡 Обновить `/admin/stats`** на backend — добавить `total_services` (опционально)
4. **🟡 Добавить кнопку деактивации** для услуг (опционально, есть workaround через Edit)
5. **🔵 Обновить документ** `session-context-admin-panel.md` — добавить `/admin/services` endpoints
